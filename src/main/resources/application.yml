spring:
  # datasource基本配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/ns4a_data_visualization?useUnicode=true&characterEncoding=utf-8
    username: root
    password: yl1999122
    type: com.alibaba.druid.pool.DruidDataSource

  # JPA相关的基本配置
  jpa:
    hibernate:
      #更新或者创建数据表结构
      ddl-auto: update
    # 控制台显示SQL语句
    show-sql: true